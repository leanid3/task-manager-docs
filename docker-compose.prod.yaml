# ============================================================================
# Task Manager - Production Override
# ============================================================================
# Prod режим с оптимизированным образом
#
# Использование:
#   docker compose -f docker-compose.yaml -f docker-compose.prod.yaml up -d
#   make up-prod
# ============================================================================

services:
  # ============================================================================
  # Task Manager Application (Prod Override)
  # ============================================================================
  task-manager:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    volumes:
      - ./logs:/app/logs
