# ============================================================================
# Task Manager - Standalone (Infrastructure Only)
# ============================================================================
# Запуск только инфраструктуры без приложения
#
# Использование:
#   docker compose -f docker-compose/base.yaml -f docker-compose/standalone.yaml --profile standalone up -d
# ============================================================================
# Standalone профиль использует только сервисы из base.yaml
# Этот файл нужен для явного указания профиля
